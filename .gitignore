# dependencies
node_modules/

# expo
.expo/
dist/
web-build/
.expo-shared/
expo-env.d.ts

# native (CNG — prebuild สร้างใหม่ได้ทุกเมื่อ)
android/
ios/
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision

# misc
.DS_Store
*.log
npm-debug.*
yarn-debug.*
yarn-error.*

# golden corpus reports (generated)
golden-report*.json

# playwright (E2E artifacts)
test-results/
playwright-report/

# freebuff tooling (local agent database — never commit)
.freebuff/
